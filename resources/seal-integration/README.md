# SEAL integration

Pointers and examples for lab infrastructure referenced in the quest:

- **taskdeck** – Claude usage + Playwright patterns (link internal repo when publishing).
- **dashboard-website-2025** – TypeScript / React reference patterns.
- **artintel1120** – account / hosting context (document internally only if needed).

Do not commit secrets or internal URLs that are not cleared for public GitHub.
