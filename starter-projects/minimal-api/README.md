# Minimal API starter (outline)

Scaffold for [CRUD example](../../examples/01-build-crud-app/README.md).

## Suggested layout

```
minimal-api/
├── package.json
├── src/
│   ├── index.ts
│   └── routes/items.ts
└── tests/items.test.ts
```

## Commands (after you scaffold)

```bash
npm install
npm run dev
npm test
```

## With Claude

1. Copy [`claude-templates/typescript/CLAUDE.md`](../../claude-templates/typescript/CLAUDE.md) to `CLAUDE.md`.
2. `/plan` the CRUD routes before implementation.

This folder is an outline; generate the actual project in your personal repo or expand here in a future quest task.
