# Playwright automation

> **Workshop home:** [`playwright/`](../../playwright/README.md) — `@playwright/test` specs, config, CI template.

This folder is a pointer. Runnable tests live under [`playwright/`](../../playwright/README.md).

**taskdeck note:** TaskDeck uses `playwright-core` as a Claude-driven browser **agent** (`src/browser/`), not a test suite. Students learn **testing** here; they study taskdeck's agent when contributing to taskdeck — see [`taskdeck-integrations/`](../../taskdeck-integrations/README.md).
